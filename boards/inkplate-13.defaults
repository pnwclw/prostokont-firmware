# Inkplate 13 (Soldered) on ESP32-S3 (with PSRAM).
# Selected via `idf.py set-target esp32s3`.
# TODO: confirm SPIRAM mode / flash size once a unit is on hand.

CONFIG_INKPLATE_BOARD_INKPLATE13=y
CONFIG_SPIRAM=y
